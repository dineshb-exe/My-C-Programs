//                                                                    Sri:
#include<stdio.h>

const int N=4;

int main(){
/*	  char str[N][100];
	int i,j,count[N];
	printf("Enter the lines: \n");
	for(i=0;i<N;i++){
		gets(str[i]);
		for(j=0;str[i][j]!='\0';j++);
		count[i]=j;
	}
	int max=count[0];
	for(i=0;i<N;i++){
		if(count[i]>max){
			max=count[i];
		}
	}
	printf("The line(s) with maximum number of characters is/are: \n");
	for(i=0;i<N;i++){
		if(count[i]==max)
		printf("Line %d: %s \n",i+1,str[i]);
	}
}
*/ printf("\%");
}

